{
  "name": "surge-gaming-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Surge Gaming Platform",
  "scripts": {
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.js --network celoSepolia",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network celoMainnet",
    "compile": "hardhat compile",
    "clean": "hardhat clean",
    "coverage": "hardhat coverage"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.4.0",
    "dotenv": "^16.3.1",
    "hardhat": "^2.19.4"
  }
}
